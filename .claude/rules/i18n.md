---
name: i18n — next-intl namespace por feature
description: Antes de escrever string PT-BR ou usar t(), consultar setup + estrutura messages/ + AppError pattern + Zod factory
paths:
  - 'app/**/*.{ts,tsx}'
  - 'components/**/*.{ts,tsx}'
  - 'features/**/*.{ts,tsx}'
  - 'lib/contracts/errors.ts'
  - 'lib/entitlements/**/*.ts'
  - 'messages/**/*.json'
  - 'i18n/**/*.ts'
---

## Princípio

`t('key')` desde primeira string PT-BR. Memória em playbook (este arquivo) pra Claude não re-decidir em cada feature.

## Setup obrigatório (gatilho: primeira string PT-BR aparece)

**Estado atual:** `next-intl@4.12.0` instalado, NÃO configurado. Wire ANTES da primeira `<Button>Salvar</Button>`.

1. **`i18n/request.ts`** — config server-side:

   ```ts
   import { getRequestConfig } from 'next-intl/server'

   export default getRequestConfig(async () => {
     const locale = 'pt-BR' // fixo dia 0 (ADR-0040 §G, decisão 13)
     return {
       locale,
       messages: {
         common: (await import(`../messages/${locale}/common.json`)).default,
         // namespaces JIT via dynamic import por feature
       },
     }
   })
   ```

2. **`next.config.ts`** — plugin:

   ```ts
   import createNextIntlPlugin from 'next-intl/plugin'
   const withNextIntl = createNextIntlPlugin('./i18n/request.ts')
   export default withNextIntl(nextConfig)
   ```

3. **`app/layout.tsx`** — `NextIntlClientProvider` envolvendo `RouteProvider`:

   ```tsx
   import { NextIntlClientProvider } from 'next-intl'
   import { getMessages, getLocale } from 'next-intl/server'

   async function DynamicShell({ children }) {
     const locale = await getLocale()
     const messages = await getMessages()
     return (
       <NextIntlClientProvider messages={messages} locale={locale}>
         <RouteProvider {...routeProps}>
           <EntitlementProvider {...entProps}>
             {children}
             <Toaster richColors closeButton position="top-center" />
           </EntitlementProvider>
         </RouteProvider>
       </NextIntlClientProvider>
     )
   }

   export default function RootLayout({ children }) {
     // <html lang={locale}> dinâmico — fallback 'pt-BR' se Next 16 async layout forçar
     return <html lang="pt-BR" ...><body>...</body></html>
   }
   ```

## Estrutura messages/ (namespace por feature)

Dia 0:

```
messages/
  pt-BR/
    common.json      ← actions, errors, validation
```

JIT por feature (criar quando feature precisar):

```
    auth.json        ← login, signup, reset-password (junto com feature login)
    billing.json     ← paywall, upgrade, plans (junto com primeiro paywall)
    programs.json    ← criar/editar/listar (junto com feature programa)
    push.json        ← notifications PWA
    email.json       ← react-email templates
    kinds.<vertical>.json  ← vertical-specific content kinds (00-PROJETO §4).
                            Ex: kinds.fitness.json, kinds.yoga.json. Override
                            por tenant via tenant_copy_overrides JIT.
```

## Como usar `t()`

**Server Components / Server Actions:**

```ts
import { getTranslations } from 'next-intl/server'

export async function MyServerComponent() {
  const t = await getTranslations('common.actions')
  return <button>{t('save')}</button>
}
```

**Client Components:**

```tsx
'use client'
import { useTranslations } from 'next-intl'

export function MyClient() {
  const t = useTranslations('common.actions')
  return <button>{t('save')}</button>
}
```

**Via wrapper (preferido em forms/toasts):**

Wrappers compostos dia 0 (`<AppForm>`, `useAppToast`, `<AppEntitlementGate>`) usam `useTranslations` internamente. Outros wrappers JIT podem expor `i18nKey` prop quando agregar valor real (regra de 3 + Vercel Academy):

```tsx
const toast = useAppToast()
toast.success('feedback.saved') // wrapper resolve `t(key)` internamente
```

`AppButton` NÃO é wrapper dia 0 (passthrough — Vercel Academy bate). Use `<Button>` shadcn direto com `t('save')` inline.

## AppError factory com i18n

`lib/contracts/errors.ts` aceita `string | I18nMessage`:

```ts
export type I18nMessage =
  | string
  | { key: string; fallback: string; metadata?: Record<string, unknown> }

// Uso server-side (ex: lib/entitlements/server.ts)
throw AppError.forbidden({
  key: 'entitlements.feature_blocked',
  fallback: 'Plan upgrade required',
  metadata: { feature: 'chatbot' },
})
```

Server loga `fallback` (EN, Sentry-friendly). Client lê `error.metadata.i18nKey` e chama `t(key)`.

Back-compat: `AppError.forbidden('msg crua')` ainda aceita string (não quebra callsites antigos).

## Zod messages — factory por callsite

**Pattern correto (Opção A — decisão 24):**

```ts
import { z } from 'zod'
import { useTranslations } from 'next-intl'

export function useEmailSchema() {
  const t = useTranslations('common.validation')
  return z.object({
    email: z.string().email({ message: t('invalid_email') }),
  })
}
```

**NÃO USAR:**

- `z.setErrorMap(...)` global em layout — hidden state, impossível debugar locale
- `.message('string crua')` — ESLint regra 11 bloqueia

## Multi-vertical — chaves descritivas neutras

Decisão 15: chaves NEUTRAS, copy fitness-shaped no VALOR.

**Certo:**

```json
{ "programs": { "title": "Programas" } }
```

**Errado:**

```json
{ "workouts": { "title": "Treinos" } } // vocab vertical-specific na chave
```

Vertical override vem via tenant_copy_overrides JIT (ver "Condição de revisitar" abaixo).

## Anti-patterns

- String hardcoded em JSX → use `{t('key')}` (ESLint `react/jsx-no-literals` bloqueia)
- String passada por prop → use `i18nKey` no wrapper
- `aria-label="Fechar"` literal → `aria-label={t('common.actions.close')}` (ESLint bloqueia)
- `toast.success('Salvo')` literal → `useAppToast().success('common.feedback.saved')` (ESLint bloqueia)
- `new Error('msg')` literal → `throw AppError.invalidInput({ key, fallback })` (ESLint bloqueia)
- `.message('texto')` em Zod → factory `useXSchema()` (ESLint bloqueia)
- Schema `tenant_copy_overrides` dia 0 → NÃO criar
- Locale switcher UI dia 0 → NÃO criar
- Rota `app/[locale]/` dia 0 → NÃO criar

## Condição de revisitar (regra Pesquisa 20 — sem isso vira documento morto)

| Gatilho                                                                                                 | Ação                                                                                                                                             |
| ------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------ |
| **Primeiro cliente internacional confirmado** (paying customer)                                         | Adicionar `messages/<locale>/<namespace>.json` espelho pt-BR + locale switcher + hreflang. Custo: ~2-4h                                          |
| **Cliente 2 com vertical diferente pede terminologia diferente** (ex: BoxClub "WOD" vs FitLab "Treino") | Migration `tenant_copy_overrides (tenant_id, key, value, locale)` + resolver merge em `i18n/request.ts`. Custo: ~2-4h (call sites já usam `t()`) |
| **SEO multi-locale obrigatório** OU **2+ locales com user-switch**                                      | Migrar `app/*` pra `app/[locale]/*` + atualizar `getLocale()` + hreflang + sitemap. Custo: ~2-4h                                                 |
| **2ª brand operacional com locale diferente**                                                           | Migration single-column `public.brands.default_locale` + resolver lê brand                                                                       |
| **PWA offline messages preciso** (Sprint 14, ADR-0014)                                                  | Pre-cache `messages/**/*.json` via Serwist. `@serwist/next` default cobre static JSON                                                            |
| **Stripe internacional ativa cliente real com moeda ≠ BRL**                                             | Junto com locale: revisar formatadores (`Intl.NumberFormat` por locale)                                                                          |

## Referências

- ADR-0040 §G — fonte autoritativa
- `docs/research/21-i18n-strategy.md` — pesquisa que sustenta
- `docs/blueprint/13-lint-enforcement.md §2.2` — 14 padrões i18n ESLint
- `lib/contracts/errors.ts` — `AppError` factory
- next-intl 4 App Router docs — https://next-intl.dev/docs/getting-started/app-router

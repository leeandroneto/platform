## Princípio

3 locales dia 0: **pt-BR + en + es**. `t('key')` desde primeira string PT-BR em chrome do app.

## Setup cravado

- `next-intl@4`
- `i18n/request.ts` carrega namespaces por feature (`common`, `auth`, `dashboard`, `meu-site`, etc)
- `next.config.ts` plugin `createNextIntlPlugin('./i18n/request.ts')`
- `app/layout.tsx` envolve com `NextIntlClientProvider`

## Estrutura `messages/`

```
messages/
├── pt-BR/
│   ├── common.json      ← actions, errors, validation universais
│   ├── auth.json        ← entrar/cadastrar/sair/reset
│   ├── dashboard.json   ← painel
│   └── ...
├── en/
│   └── ... (espelho)
└── es/
    └── ... (espelho)
```

## Uso

### Server Components / Server Actions

```ts
import { getTranslations } from 'next-intl/server'

export async function MyServerComponent() {
  const t = await getTranslations('common.actions')
  return <button>{t('save')}</button>
}
```

### Client Components

```tsx
"use client";
import { useTranslations } from "next-intl";

export function MyClient() {
  const t = useTranslations("common.actions");
  return <button>{t("save")}</button>;
}
```

## AppError com i18n key

```ts
// lib/contracts/errors.ts aceita string | I18nMessage
throw AppError.forbidden({
  key: "entitlements.feature_blocked",
  fallback: "Plan upgrade required",
  metadata: {
    feature: "theme_builder",
    current_plan: "free",
    required_plan: "apoiador",
  },
});
```

Server loga `fallback` (EN). Client lê `error.metadata.i18nKey` e chama `t(key)`.

## Conteúdo gerado por tenant — NÃO usa `t()`

Strings escritas pelo dev (chrome do app) usam `t()`. Strings escritas pelo profissional/IA no editor de site são **dado do tenant**, ficam **inline no JSONB** do `page_versions.content.slots`.

### Mapa de decisão

| Tipo de texto                               | Onde mora                                   | Quem escreve       |
| ------------------------------------------- | ------------------------------------------- | ------------------ |
| Botão "Salvar", menu "Configurações"        | `messages/pt-BR/*.json`                     | Dev                |
| Erro "E-mail inválido", "Campo obrigatório" | `messages/pt-BR/*.json`                     | Dev                |
| Headline do hero do site do tenant          | `page_versions.content.slots.hero.headline` | Tenant / IA (chat) |
| Bio do treinador                            | `coaches.bio jsonb`                         | Tenant / IA        |
| Nome do plano "Iniciante 12 semanas"        | `plans.name` (column)                       | Tenant             |

### Paths onde literal PT-BR/EN é PERMITIDO em JSX

```
components/site/sections/**     # consome JSONB do page_versions
components/site/templates/**    # consome JSONB do page_versions
lib/seed/**                     # seeds com copy retake embutida
```

ESLint regra `react/jsx-no-literals` desabilitada nesses paths.

## Multi-idioma de conteúdo do tenant (futuro)

Quando tenant quiser site em pt-BR + EN, spec ganha overlay:

```jsonc
{
  "slots": { ... },
  "translations": {
    "en": { "hero.headline": "Train smarter, run faster" }
  }
}
```

Renderer aplica overlay quando `?lang=en` na URL. Sem dependência de `messages/*`.

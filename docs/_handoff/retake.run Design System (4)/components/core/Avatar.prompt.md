Round athlete/coach avatar — photo or initials fallback, optional status dot.

```jsx
<Avatar name="Marina Costa" size={44} status="online" />
<Avatar src="/p.jpg" name="João" ring />
```

Falls back to display-font initials on an ocean tint when `src` is absent. `status`: `online | paused | risk`.

#!/usr/bin/env sh
# Husky pre-push — full test + grep audits (Husky v9 — sem linha legacy)
pnpm exec bash scripts/vocab-audit.sh
pnpm exec bash scripts/token-audit.sh
pnpm exec bash scripts/grep-disables.sh
pnpm exec tsx scripts/docs-validate.ts
pnpm exec vitest run
